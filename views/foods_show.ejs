<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>GitPub</title>
    <style type="text/css">
        body {
        color: blanchedalmond;
        background-color: steelblue;
        }
    </style>
</head>
<body>
    <h1>At foo bar</h1>
    <h2><%=food.name[0].toUpperCase() + food.name.substring(1) %></h2>
    <h3><%=food.price%> Dollars</h3>
    <img src="<%=food.image.split('.j')[0]+'.png'%>" alt="" ><br>
    <a href="/foods">Main Menu</a>
</body>
</html>