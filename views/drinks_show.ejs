<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>GitPub</title>
    <style type="text/css">
        body {
        color: blanchedalmond;
        background-color: steelblue;
        }
    </style>
</head>
<body>
    <h1>At foo bar</h1>
    <h2><%=drink.name[0].toUpperCase() + drink.name.substring(1) %></h2>
    <h3><%=drink.price%> Dollars</h3>
    <img src="<%=drink.image.split('.j')[0]+'.png'%>" alt="" ><br>
    <a href="/drinks">Main Menu</a>
</body>
</html>